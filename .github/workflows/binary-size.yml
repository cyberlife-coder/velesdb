name: Binary Size Gate

# Verifies the "~9 MB binary" promise the README makes (QUALITY_BAR.md
# "Additional gates"). The headline figure used to be prose only — nothing
# measured it, so a heavy dependency could have inflated the binary while the
# claim went stale. This job builds the stripped release binaries and fails if
# any exceeds its ceiling (see scripts/check_binary_size.py for the values and
# the headroom rationale).
#
# Note: ceilings carry headroom over the Apple-Silicon headline figure to
# absorb the Linux x86_64 build delta this CI runner produces. They catch a
# real regression (a multi-MB dependency, a bundled asset), not exact bytes.

# Called by ci.yml (see the note in gate-contracts.yml) on pull requests AND on
# pushes to develop/main -- ci.yml's own `push:` filter is a strict superset of
# the paths this gate cares about. The `pull_request` trigger is gone so the
# ceiling reports inside `CI Success`, the only required check.
#
# The standalone `push:` trigger is gone too, and its removal is a fix rather
# than a loosening. It fired on the same develop/main pushes as ci.yml, and
# both instances landed in the concurrency group below -- which, unlike
# propagation-guard.yml's, does not carry `github.workflow`, so the two
# collided and one killed the other. It was ALWAYS the standalone that died:
# 60 of the last 60 standalone runs are `cancelled`, and the gate has never
# once completed in this form. A workflow whose entire history reads
# `cancelled` tells a reader the gate is broken; keeping it would also leave a
# race that could one day cancel the ci.yml instance instead, turning a clean
# tree red on a required check.
#
# Coverage is unchanged, and that is checkable rather than asserted: every path
# the old filter listed is matched by ci.yml's `push:` paths (`Cargo.toml`,
# `Cargo.lock`, `crates/**`, `scripts/**`, `.github/workflows/**`).
on:
  workflow_call:
  workflow_dispatch:

concurrency:
  group: binary-size-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always

jobs:
  binary-size:
    name: Release binaries within ceilings
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # stable
        with:
          toolchain: "1.90"
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.11"
      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        with:
          shared-key: "velesdb-binary-size"

      - name: Build stripped release binaries
        # Default features = the configuration the published artifacts ship.
        # `[profile.release]` in Cargo.toml sets strip = true, lto = true.
        run: cargo build --release -p velesdb-server -p velesdb-cli -p velesdb-migrate

      - name: Check binary sizes against ceilings
        # `set -o pipefail` is load-bearing, not hygiene. GitHub's default shell
        # is `bash -e {0}`, where a pipeline's exit status is the LAST command's
        # — so `check_binary_size.py | tee` reported tee's 0 and this job, a
        # required check through `CI Success`, passed on every over-ceiling
        # binary it ever measured. It did exactly that on #2193: the script
        # printed `Binary size gate FAILED` for two binaries under a green
        # check. Reachability keeps this honest
        # (`test_a_strict_guard_is_not_disarmed_by_a_pipe`).
        #
        # The status is captured rather than allowed to abort the step, so the
        # report still reaches the run summary on the run where it matters.
        run: |
          set -o pipefail
          status=0
          python scripts/check_binary_size.py --target-dir target/release \
            | tee binary-size-report.txt || status=$?
          {
            echo "## Binary Size Gate"
            echo ""
            echo '```'
            cat binary-size-report.txt
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
          exit "$status"

      - name: Upload size report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: binary-size-report-${{ github.sha }}
          path: binary-size-report.txt
          retention-days: 30
