[package]
name = "tauri-plugin-velesdb"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
description = "Tauri plugin for VelesDB - Vector search in desktop apps"
license-file = "LICENSE"
repository = "https://github.com/cyberlife-coder/VelesDB"
keywords = ["tauri", "plugin", "vector", "database", "search"]
categories = ["database", "gui"]
links = "tauri-plugin-velesdb"

[build-dependencies]
tauri-plugin = { version = "2.6", features = ["build"] }

[dependencies]
tauri = { version = "2.11", default-features = false }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
tracing = "0.1"
parking_lot = "0.12"
dirs = "6.0"

# VelesDB core dependency (local path for development)
velesdb-core = { workspace = true }

[dev-dependencies]
tempfile = "3.14"
toml = "1.1"
# Used by tests/nonblocking_state.rs to build a single-worker async runtime
# and prove blocking core operations never starve runtime workers.
tokio = { version = "1.52", features = [
    "rt-multi-thread",
    "macros",
    "time",
    "sync",
] }
# `test` feature unlocks `tauri::test::{mock_builder, mock_context, noop_assets}`
# and `MockRuntime`, used by command integration tests to build an
# `App<MockRuntime>` and exercise the `#[command]` handlers directly.
tauri = { version = "2.11", default-features = false, features = ["test"] }

[features]
default = ["velesdb-core/default"]
bench-sift1m = ["velesdb-core/bench-sift1m"]
## Forwards the `velesdb-core/bytemuck` compatibility stub (see that crate's
## Cargo.toml) so this parity table stays a straight mirror, not an
## exception list.
bytemuck = ["velesdb-core/bytemuck"]
gpu = ["velesdb-core/gpu"]
internal-bench = ["velesdb-core/internal-bench"]
loom = ["velesdb-core/loom"]
openapi = ["velesdb-core/openapi"]
persistence = ["velesdb-core/persistence"]
## Forwards `velesdb-core/test-fault-injection`. Only enable for
## integration tests that exercise rollback paths. Never include
## in production Tauri bundles.
test-fault-injection = ["velesdb-core/test-fault-injection"]
update-check = ["velesdb-core/update-check"]

[lints]
workspace = true
