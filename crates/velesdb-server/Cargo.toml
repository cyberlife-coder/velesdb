[package]
name = "velesdb-server"
description = "REST API server for VelesDB vector database"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license-file = "../../LICENSE"
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
keywords.workspace = true
categories.workspace = true
authors.workspace = true

[lib]
name = "velesdb_server"
path = "src/lib.rs"

[[bin]]
name = "velesdb-server"
path = "src/main.rs"

[dependencies]
velesdb-core = { workspace = true, default-features = false, features = [
    "openapi",
    "persistence",
] }
tokio = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
axum = { workspace = true }
tower = { workspace = true }
tower-http = { workspace = true }
futures = "0.3"
clap = { version = "4.6", features = ["derive", "env"] }
toml = { workspace = true }
parking_lot = "0.12"

# Rate limiting
tower_governor = "0.8"
governor = "0.10"

# TLS support via rustls
rustls = { version = "0.23", default-features = false, features = [
    "ring",
    "logging",
    "std",
    "tls12",
] }
tokio-rustls = "0.26"
rustls-pemfile = "2"
hyper = "1"
hyper-util = { version = "0.1", features = ["tokio", "server-auto"] }

# OpenAPI/Swagger documentation
utoipa = { version = "5", features = ["axum_extras"] }
utoipa-swagger-ui = { version = "9", features = ["axum"], optional = true }

[features]
default = ["velesdb-core/default", "persistence", "update-check", "prometheus"]
bench-sift1m = ["velesdb-core/bench-sift1m"]
## Forwards the `velesdb-core/bytemuck` compatibility stub (see that crate's
## Cargo.toml) so this parity table stays a straight mirror, not an
## exception list.
bytemuck = ["velesdb-core/bytemuck"]
gpu = ["velesdb-core/gpu"]
internal-bench = ["velesdb-core/internal-bench"]
loom = ["velesdb-core/loom"]
openapi = ["velesdb-core/openapi"]
## `velesdb-core/persistence` is already forced on unconditionally via the
## `[dependencies]` entry above (like `openapi`), because velesdb-server's own
## code uses persistence-gated core APIs (index management, admin
## diagnostics, graph handlers, etc.) without matching cfg gates throughout.
## This feature is kept as a no-op alias for backward compatibility with
## existing `--features persistence` invocations (see #1472).
persistence = ["velesdb-core/persistence"]
## Serves GET /metrics (Prometheus exposition format). Default since 1.19:
## released binaries and the Docker image expose operational metrics.
prometheus = []
update-check = ["velesdb-core/update-check"]
swagger-ui = ["dep:utoipa-swagger-ui"]
## Forwards `velesdb-core/test-fault-injection`. Only enable for
## integration tests that exercise rollback paths via
## `SaveConfigFaultGuard`. Never include in production builds.
test-fault-injection = ["velesdb-core/test-fault-injection"]

[dev-dependencies]
# Use native-tls to avoid ring compilation issues on aarch64-apple-darwin
reqwest = { version = "0.13", default-features = false, features = [
    "json",
    "native-tls",
] }
tempfile = { workspace = true }
tower = { workspace = true }
toml = { workspace = true }
serde_yaml = "0.9"

[lints]
workspace = true
