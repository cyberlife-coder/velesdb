# =============================================================================
# Git Line Ending Configuration
# =============================================================================
# Strategy: LF in repo (Unix), auto-convert on checkout/commit
# This ensures CI (Linux) and local dev (Windows) work seamlessly.
# =============================================================================

# Default: auto-detect text files and normalize to LF
* text=auto eol=lf

# Rust source files - always LF (matches rustfmt)
*.rs text eol=lf
*.toml text eol=lf
*.md text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.json text eol=lf

# Shell scripts - always LF (required for Unix execution)
*.sh text eol=lf

# Windows batch files - always CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Binary files - no conversion
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.db binary
